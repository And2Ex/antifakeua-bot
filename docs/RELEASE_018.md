# Release 018 — Sandbox launch payments and UI cleanup

- Повернено повні ціни пакетів без акційної знижки.
- Для sandbox-режиму LiqPay додано чесне повідомлення: кошти не списуються, пакет активується для тестування.
- Покращено користувацькі повідомлення: ліміти, оплата, відгуки, довідка, команда `/check`.
- Збережено стримане HTML-форматування без зайвих емодзі.
- Архів має містити тільки вихідний код, без `.venv`, `__pycache__`, `.pyc`, `.env`, бази даних та службових файлів IDE.
